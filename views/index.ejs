<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="/styles.css">
    <title>Message Board</title>
</head>

<body>
    <div class="container">
        <header>
            <h1>💬 Message Board</h1>
            <p class="subtitle">Share your thoughts with the community</p>
        </header>
        <% messages.forEach((message, i)=> { %>
            <div class="message-card">
                <div class="message-header">
                    <div class="author">
                        <%= message.user %>
                    </div>
                    <div class="timestamp">
                        <%= message.added.toDateString() %>
                    </div>
                </div>
                <div class="message-content">
                    <%= message.text %>
                </div>
            </div>
        <% }); %>
        <button class="message-card" onclick="window.location.href='/new'">
            <div 
            class="message-content"
            style="cursor:pointer;"
            >
                <b>Add a new messaage</b>
            </div>
        </button>
    </div>
</body>

</html>